extends layout

block content
    h1 #{__('ADDCOL')}
    form(method='POST', action='/cards/edit/'+card.id)
        #form-group
            label #{__('DEF')}
            input.form-control(name='front', type='text')
        #form-group
            label #{__('EXPL')}
            textarea.form-control(name='back')
        #form-group
            label #{__('TRNSL')}
            textarea.form-control(name='transl')
        #form-group
            label #{__('EXMPL')}
            textarea.form-control(name='exmpl')
        br
        input.btn.btn-primary(type='submit',value='OK')