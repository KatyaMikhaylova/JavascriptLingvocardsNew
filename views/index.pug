extends layout

block content
    h1 #{__('WELCOME')}
    .container
        .row
        each card, i in cards
            .col-md-4.noa
                a(href='/lingvo/' + card._id)
                    .cover
                        p=card.cards[0].front
                    p.name=card.title




